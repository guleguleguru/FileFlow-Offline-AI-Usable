# Gemini Instructions

Follow `AGENTS.md` for this repository.

Use FileFlow Offline through `offline-converter-agent.exe` for local file conversion. Always request JSON output, parse `tasks[].outputs`, and verify generated files exist before reporting success.
