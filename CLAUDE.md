# Claude Instructions

Follow `AGENTS.md` for this repository.

Key points:

- Use `python skills/fileflow-offline-ai/scripts/resolve_fileflow.py` to locate FileFlow Offline.
- Use `offline-converter-agent.exe` for automation and `--json` output.
- Never upload user files. Convert locally and validate output paths.
- Do not commit bundled runtimes, release zips, local documents, or conversion outputs.
